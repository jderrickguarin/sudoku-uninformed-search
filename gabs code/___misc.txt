Sub PrintArray(Data As Variant, Cl As Range)
    Cl.Resize(UBound(Data, 1), UBound(Data, 2)) = Data
End Sub

Sub Macro1()

    Dim myArr As Variant
    myArr = Range("A1:B2")

    PrintArray myArr, ActiveWorkbook.Worksheets("Sheet1").[A5]
    
End Sub

Sub arraylistorsomethn()

    'http://www.snb-vba.eu/VBA_Arraylist_en.html
    
    Dim bfsqueue As Object
    Set queue = CreateObject("System.Collections.ArrayList")
    
    coll.Add myArr
    
End Sub
